<% @posts.each do |post| %>
  <div class="section">
    <h2 class="title left-align"><%= link_to post.title, post%></h2>
    <p class="date left-align"><%= post.created_at.strftime("%B, %d, %Y") %></p>
  </div>
<% end %>
